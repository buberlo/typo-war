{
  "name": "word-shooter",
  "private": true,
  "version": "1.0.0",
  "description": "Local two-player Phaser word shooter where typed words become projectiles and aggressive autocorrect rewrites them mid-flight.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "phaser": "^3.85.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}